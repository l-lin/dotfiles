---
description: Implement plan stored in `.sandbox/plans/`.
argument-hint: "<plan-file-path> [additional-instruction]"
---

Implement the work described in $1.
Run the full test suite once at the end. Use `ask-user-question` tool if there are any points to clarify.
$2
