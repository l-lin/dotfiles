/**
 * Keep submitted prompts literal.
 *
 * Shared snippet definitions still live in ./snippets.ts for awesome-editor
 * autocomplete and Ctrl-E expansion.
 */
import type { ExtensionAPI } from "@earendil-works/pi-coding-agent";

export default function (_: ExtensionAPI) {}
